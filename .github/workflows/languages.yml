name: Refresh public language card
on:
  schedule:
    - cron: '23 1 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['scripts/update_languages.py', '.github/workflows/languages.yml']
permissions:
  contents: write
concurrency:
  group: profile-language-card
  cancel-in-progress: false
jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Generate language card
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 scripts/update_languages.py
      - name: Save updated card
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add README.md assets/languages.svg
          if ! git diff --cached --quiet; then
            git commit -m "Refresh public language breakdown"
            git push
          fi
